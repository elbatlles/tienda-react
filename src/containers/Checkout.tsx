import React, { PureComponent } from 'react'

interface Props {
    
}

export default class Checkout extends PureComponent<Props> {
    render() {
        return (
            <div>
                <h1> Checkout</h1>
            </div>
        )
    }
}
